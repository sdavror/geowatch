# Entity Resolution — registry & sanctions-source coverage

Working reference for the Entity Resolution Engine's source expansion. Every
row was checked against the live endpoint from this host before being
marked buildable — "researched, blocked" means the endpoint was actually
tried and failed, not assumed to fail. Update this table as sources are
added or new candidates are researched.

## Live (shipped)

| Source | Country/scope | Type | Auth | PR |
|---|---|---|---|---|
| OFAC SDN | US sanctions (RU/UA/BY scope) | sanctions | keyless | #36 |
| GLEIF | global (LEI registry + ownership) | registry | keyless | #36, #40 |
| EU Consolidated Sanctions | EU | sanctions | keyless | #40 |
| UK OFSI | UK sanctions | sanctions | keyless | #40 |
| US SEC EDGAR | US public companies | registry | keyless (descriptive User-Agent required) | #41 |
| UK Companies House | UK | registry | free API key (user-registered) | #42 |
| France SIRENE/RNE | France | registry | keyless | #43 |

## Built this round (`feature/registry-expansion`) — all four ingested live against production data

| Source | Country/scope | Type | Auth | Result |
|---|---|---|---|---|
| Canada Consolidated Autonomous Sanctions List | Canada | sanctions | keyless | Bulk XML, same shape as EU/OFSI, no structured identifiers (relies on Phase 2/3 fuzzy+LLM). Live run: **1,756 processed → 1,756 new entities, 0 auto-merged** (correct — Phase 2 never auto-merges); queued **811 pending merge reviews**, e.g. a real 100%-confidence "BANK ROSSIYA" vs "Bank Rossiya" duplicate pair correctly held for human approval rather than silently merged or duplicated. ~14 min run time (LLM gray-zone budget is the bottleneck). |
| Australia DFAT Consolidated List | Australia | sanctions | keyless | Ships as XLSX (1.3MB) — added the `xlsx` (SheetJS) dependency, the repo's first. Rows grouped by base `Reference` number (alias suffix stripped), same pattern as OFSI's GroupID. Live run: **459 processed → 459 new entities, 0 auto-merged**. ~2.5 min run time. |
| UK Companies House PSC (beneficial ownership) | UK | registry extension | reuses existing free API key | Not a new registry — extends the existing Companies House adapter with real "Persons with Significant Control" data. Corporate-entity PSCs link as `EntityRelationship` parents (same shape as GLEIF's parent/child). Individual-person PSCs (real people) are deliberately **not** modelled — `Entity.entityType` only supports `company`; adding a Person concept is a schema decision, not a source-addition decision, so those are left in the raw payload for a future round. Verified live against 3 real sanctioned UK entities (Rosneft Marine UK, Sberbank CIB UK, Surgutneftegas Ltd) — correct `kind` discrimination each time (2 individual PSCs correctly skipped, 0 corporate PSCs present in this sample so the relationship-linking arm itself wasn't exercised live, though it reuses the same Prisma upsert already proven correct by the GLEIF relationship path). |
| US Consolidated Screening List (trade.gov) | US, aggregates 11 non-SDN export/sanctions lists (BIS Entity List, Denied Persons List, ITAR debarred, Treasury SSI, etc.) | sanctions | free API key (user self-registered at developer.trade.gov) | SDN itself deliberately excluded — 100% redundant with the existing dedicated `ofac-sdn.adapter.ts`. **Real endpoint quirks found only by testing live**, not from the docs: (1) hostname in the actual "Try this operation" console is `data.trade.gov`, not `api.trade.gov` from the docs prose; (2) `types=Entity` filter is silently broken for 7 of the 13 lists (returns 0 even for lists that are almost entirely companies, e.g. BIS Entity List) — classify Individual vs Entity client-side instead, from birth/citizenship/nationality field presence; (3) hard pagination ceiling of offset+size≤1050 per query, worked around by splitting per (source × country) — one combo (Entity List × Russia, 1,094 records) still exceeds it by 44, logged not silently dropped. **Two real bugs found and fixed via live verification**: an unpaced probe-then-paginate loop hit persistent 429s that were silently treated as "0 results" (a first live run under-counted several genuinely non-empty lists, e.g. SSI came back empty) — fixed with retry+backoff and per-request pacing; a `crimea (occupied)` free-text country label (this API's Crimea convention, distinct from OFAC's own "Region: Crimea") blew the `CHAR(2)` `primaryCountryId` column and crashed the whole ingestion run partway through — fixed with the same normalize-known-aliases pattern as `OFAC_COUNTRY_NAME_ALIASES`. Corrected live run: **1,269 processed → 615 new entities, 654 merged via real exact-identifier matches** (much higher merge rate than Canada/Australia, confirming CSL's structured `ids[]` genuinely cross-reference existing OFAC/GLEIF data) — verified a clean new-entity case end-to-end (`AKTSIONERNOE OBSHCHESTVO RT-KHIMICHESKIE TEKHNOLOGII I KOMPOZITSIONNYE MATERIALY`, 3 correct `reg_number@RU` identifiers, `US CSL (SSI)` sanction, correct source attribution). Pending-review queue grew from 811 to **1,950** after this run. |

## Built round 3 (`feature/baltic-registries`) — beneficial-ownership registries, not sanctions lists

Same RU/UA/BY-scoping convention as every sanctions source, applied to a different kind of data: instead of "is this entity itself designated," these ask "does this company have a Russian/Ukrainian/Belarusian beneficial owner" — a real ownership-network signal for shell-company/sanctions-evasion research, distinct from a designation. Individual beneficial owners (real people) are never modelled as Entities, same reasoning as Companies House PSC.

| Source | Country/scope | Type | Auth | Result |
|---|---|---|---|---|
| Estonia e-Business Register | Estonia | beneficial ownership | keyless bulk download | Free JSON bulk file (`kasusaajad.json.zip`, self-contained — company name + beneficial owners in one record, no join needed). **Real bug found live**: this Cloudflare-fronted host returns 403 specifically to Node's native `fetch()` (undici) while `wget`/`curl` from the exact same container get 200 — confirmed by testing both from inside the running container against the identical URL; adding browser-like headers to fetch() didn't help either, pointing to a TLS/HTTP-client fingerprint block rather than a UA or network-path issue. Fixed by shelling out to `wget` for this one download. Live run: **4,422 processed → 4,422 new entities** (1,176 of the underlying 374k-company file have an RU-resident beneficial owner alone). Verified live (`2Circas OÜ`, correct `reg_number@EE`). |
| Latvia Enterprise Register | Latvia | beneficial ownership | keyless bulk download | Two separate free CSVs (beneficial owners + main register) requiring a two-file join — the owners file only has a registration number, not a company name. Owners file carries clean ISO2 `nationality` *and* `residence` (richer than Estonia's residence-only). **Real bug found live**: the 128MB register.csv download died mid-transfer with undici's `TypeError: terminated` on the first live run (silent connection drop, not an HTTP error) — fixed with the same fetch-with-retry shape as the OFSI adapter. Live run: **3,862 processed → 3,862 new entities**. Verified live (`SIA Tour Up in Europe`, correct `reg_number@LV`). |

## Researched, blocked (same category as UN sanctions / GDELT — revisit later)

| Source | Country/scope | Type | What was tried | Result |
|---|---|---|---|---|
| Switzerland SECO (SESAM) | Switzerland | sanctions | Every XML endpoint variant found via search, incl. the exact URL WebFetch extracted from the SECO page's own rendered content (`sesam.search-admin.ch/...`) | **Confirmed dead, not just blocked**: `nslookup sesam.search-admin.ch` returns NXDOMAIN — the domain doesn't exist. WebFetch was extracting a stale/wrong URL from page content each time, not a real live link. Would need someone to actually click through the live site's JS-driven download button to find the current real endpoint — not resolvable by URL-guessing or search-engine caching. |
| Czech ARES | Czechia | registry | `ares.gov.cz/ekonomicke-subjekty-v-be/rest/...` and `/ekonomicke-subjekty/v3/...`, with and without browser User-Agent | 403 "The request is blocked" (WAF) on every attempt — looks like bot/geo-blocking, not a UA issue (the OFSI/state.gov browser-UA fix didn't help here). |
| Ireland CRO | Ireland | registry | `api.cro.ie/` | 403 — likely requires a registered API key; not yet investigated further. |
| Poland KRS | Poland | registry | `api-krs.ms.gov.pl/api/krs/OdpisAktualny/{krsNumber}` | **Works**, but lookup-only by known KRS number (confirmed real data incl. NIP/REGON for e.g. ORLEN, KRS 0000028860) — no name-search endpoint found (`/api/krs/Wyszukaj?nazwa=...` → 404). Not usable for our search-by-name enrichment flow without a separate name→KRS-number bridge (e.g. GUS REGON search). Revisit if a search endpoint turns up. |
| Lithuania (Registrų centras / JADIS) | Lithuania | registry | Found a real, live, keyless bulk file (`registrucentras.lt/aduomenys/?byla=jadis_ad_dalyviu_salis.csv`) via data.gov.lt | **Wrong shape, not blocked** — this file is aggregate statistics (participant counts by country + legal form + period), not per-company records. No company names or registration numbers in it at all, so it can't feed Entity records the way Estonia's/Latvia's per-company files do. The genuine per-entity JADIS data appears to require the authenticated real-time API, not a bulk export. |
| Netherlands KVK | Netherlands | registry | Full pricing/access docs read | Confirmed structurally unusable for us, not just gated: production API access explicitly requires the requester to have a subsidiary or registered entity in the Netherlands — "foreign organisations without a Dutch presence cannot access the KVK API directly." The free test environment only serves fictitious data. |
| Cyprus Registrar of Companies | Cyprus | registry | Official eSearch portal, OpenCorporates, third-party aggregators | No free bulk/API found — official search is manual-only (paid €10 detailed search), no open-data bulk export located this round. High-value target (common RU-shell jurisdiction) — worth a deeper look in a future round, not confirmed fully dead the way Switzerland/KVK are. |
| Georgia (NAPR) | Georgia | registry | Search only | Confirmed no public API exists at all (not even gated) — portal is web-search-only. |
| UAE (National Economic Register + emirate DEDs) | UAE | registry | Search only | Confirmed no public API from any mainland DED or the federal NER — UAE has no single central registry (per-emirate + free-zone licensing), all commercial aggregators are paid. |
| Turkey (MERSİS) | Turkey | registry | Search only | Confirmed no public API — registered-user/e-signature portal access only, no bulk or programmatic option found. |

## Built round 4 (`feature/global-registry-expansion`) — general on-demand registries beyond the EU/English-speaking world

The user's stated end goal is genuinely global coverage (explicitly called out Japan as an example gap). This round's 4 priority candidates (Japan, Brazil, Denmark, South Korea) all hit real, documented obstacles — but two adjacent Nordic candidates (Norway, Finland) turned out to be excellent free/keyless on-demand registries, same shape as Companies House/France SIRENE.

| Source | Country/scope | Type | Auth | Result |
|---|---|---|---|---|
| Norway Brønnøysund Register (Enhetsregisteret) | Norway | registry, on-demand search-by-name | keyless | Free, keyless, real-time REST API (`data.brreg.no`) — confirmed live 2026-07-20. One search call gives name/address/industry/**website** (a field most other sources lack entirely); a separate `/roller` endpoint gives real reported board members and daily manager (e.g. verified against Equinor ASA — CEO "Anders Opedal" as `Daglig leder`, plus board). Status normalized from three real boolean fields (`konkurs`=bankrupt→liquidated, `underAvvikling`/`underTvangsavviklingEllerTvangsopplosning`→dissolved). Best general-registry source found so far — better field coverage than Companies House. |
| Finland PRH YTJ v3 | Finland | registry, on-demand search-by-name | keyless | Free, keyless, real-time REST API (`avoindata.prh.fi`) — confirmed live 2026-07-20, name search and businessId lookup both work. Gives website, industry code **with an English-language description** (`mainBusinessLine.descriptions`), and address. **Deliberately left `status` unset**: the API returns numeric codes (`status`, `tradeRegisterStatus`) with no documented enum found anywhere in the public docs — guessing a mapping risked silently wrong data, so this field stays null pending a real source for the code meanings (same "don't invent meanings for unclear fields" discipline as the France/Companies House status mapping). No officer/director data exposed by this API, unlike Norway's `/roller`. |
| Switzerland Zefix (Central Business Names Index) | Switzerland | registry, on-demand search-by-name | keyless (undocumented frontend API) | The **official documented API** at `zefix.admin.ch` requires a paid subscription (confirmed live: 401 without a key). Instead uses the `www.zefix.ch` frontend's own internal API — undocumented, but confirmed live 2026-07-20 with no key/auth at all. Real UID (Swiss registration-number equivalent), registered canton/commune, and status (`EXISTIEREND`→active, `IN_AUFLOESUNG`→dissolved — both confirmed by searching a real defunct company, Swissair's various liquidating entities). Deliberately sparse: Zefix is a name INDEX, not the full commercial register — no street address, industry code, website, or officers. Worth building despite the gaps given Switzerland's real-world relevance as a common holding/trading jurisdiction. Caveat: being undocumented, this endpoint could change without notice. |

## Researched, blocked this round (Japan/Brazil/Denmark/South Korea priority candidates)

| Source | Country/scope | Type | What was tried | Result |
|---|---|---|---|---|
| Japan NTA Corporate Number API (houjin-bangou.nta.go.jp) | Japan | registry | Live endpoint probing, then the official webapi docs page | Real API exists but **every request requires a free "application ID"** issued only after a **2–4 week** registration wait (not same-day self-service like Companies House/trade.gov) — not buildable/verifiable in a single session. A keyless bulk CSV of the full ~8M-company register also exists, but with no RU/UA/BY (or any) scoping signal of its own, bulk-ingesting all 8M rows would be wildly disproportionate to this project's on-demand enrichment use case for general registries. Deferred until the user wants to start the multi-week registration process. |
| Brazil Receita Federal CNPJ (via minhareceita.org) | Brazil | registry | Live-tested the real free/keyless API against a known real CNPJ (Petrobras) — works, and returns genuinely rich data including `qsa` (real shareholders/directors with roles like "Diretor"). Then checked the full documented endpoint list. | **Wrong shape, not blocked**: confirmed via its own docs — direct CNPJ lookup only, plus filterable bulk search by state/CNAE/legal-form/shareholder-CPF, but **no name-search endpoint exists**. Without already knowing a company's CNPJ, there's no way to discover it from a canonical name the way Companies House/France/Norway/Finland's search-by-name flow requires. Revisit if a future need arises where the CNPJ is already known (e.g. cross-referencing via a shareholder CPF from another source). |
| Denmark CVR (via cvrapi.dk) | Denmark | registry | Live search request with several User-Agent formats per the docs' recommendations | Persistent 403 regardless of UA — this free third-party mirror is capped at 50 lookups/day even with a registered token, and appears to already be rate-limiting/blocking this host's IP. The real official CVR data requires registering with Erhvervsstyrelsen (Virk) for API access, a heavier lift than a quick self-service key. Deferred — not confirmed permanently dead, just not viable to build+verify live today. |
| South Korea DART (opendart.fss.or.kr) | South Korea | registry (public/listed companies only) | Docs review; confirmed the real endpoint responds (`crtfc_key` param, proper "invalid key" error rather than 404) | Requires a **self-registered free API key** — same category as Companies House/trade.gov in principle, but (a) DART only covers publicly listed companies' financial disclosures, much narrower than a general company registry, and (b) actually obtaining the key needs the user to create an account on a Korean-language government portal. Needs the user to self-register before this is buildable, same as the trade.gov/Companies House keys were. |

| Slovakia ORSF register aggregator | Slovakia | registry, on-demand search-by-name | keyless | The official RPO API (`api.statistics.sk/rpo/v1`) responded but kept rejecting every query-parameter name tried with a generic "no parameters specified" error, and no working parameter name was found in the time available. Used `api.orsf.sk` instead — a third-party aggregator over the official RPO/ORSR/RUZ sources, free and keyless, confirmed live 2026-07-20 with real search-by-name and full profile (street address, postal code, NACE industry code, status). Director data exists in the provider's schema (`roles`) but its own docs say it requires authentication — comes back empty, so left unmapped rather than reported as "no directors". |

Also briefly probed: **Iceland** (rsk.is) — HTML search portal only, no API endpoint found; not investigated further this round. **New Zealand** (NZBN API via api.business.govt.nz) — real documented API exists but needs a self-registered subscription key on the MBIE developer portal, and the docs note "the API support team may request further information before the subscription can be approved" — not guaranteed same-day self-service like Companies House/trade.gov, so not built this round. **Croatia** (sudreg-api.pravosudje.hr) — request failed to connect entirely (not even an HTTP error), not investigated further.

## Candidates not yet probed (next research round)

- Kazakhstan (`data.egov.kz` has a documented API v4, elasticsearch-backed — found but not yet tested live; fields look thinner than Estonia/Latvia's, e.g. no clear beneficial-owner nationality field)
- US BIS Entity List / Denied Persons List as **standalone** feeds (fallback if the trade.gov CSL aggregator proves unreliable)
- UN Security Council Consolidated List — previously deferred (protected API gateway), not re-tried this round
- Lithuania's genuine per-entity JADIS API (authenticated, not the aggregate bulk file found this round) — worth investigating the registration process, since the underlying data clearly exists
- Beneficial-ownership registries beyond UK PSC/Estonia/Latvia — Lithuania (see above), a few other EU states still publish UBO data post-CJEU access restrictions
